{
  // https://nuxt.com/docs/guide/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions" : {
    "moduleResolution": "node",
    "module": "es2022", // Veturエラー「Top-level 'await' expressions are only allowed」の解消のため
    "target": "es2018" // Veturエラー「Top-level 'await' expressions are only allowed」の解消のため
  }
}
